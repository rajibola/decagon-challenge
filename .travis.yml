language: node_js
node_js:
  - 'stable'

  env:
  global:
  - NODE_VERSION=stable
  branches:
    only:
    - main
  cache:
    directories:
    - node_modules
  before_install:
  - npm update
  install:
  - npm install
  script:
  - npm test
  - npm run coveralls